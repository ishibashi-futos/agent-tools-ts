{
  "name": "agent-tools-ts",
  "module": "index.ts",
  "version": "0.2.0",
  "main": "./src/lib.ts",
  "types": "./src/lib.ts",
  "exports": {
    ".": "./src/lib.ts"
  },
  "files": [
    "src",
    "README.md"
  ],
  "type": "module",
  "scripts": {
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit ",
    "sanity": "bun run format && bun run typecheck && bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.0",
    "@types/bun": "latest",
    "openai": "^6.22.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
